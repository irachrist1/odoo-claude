# Windows scripts must keep CRLF — a .bat with bare LF can misparse on some systems,
# and this holds even when the bundle is zipped up from a Mac.
*.bat text eol=crlf
*.ps1 text eol=crlf

*.sh text eol=lf
*.py text eol=lf
